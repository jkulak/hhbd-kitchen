#!/usr/bin/env bash
#
# SSH wrapper for deploying from private Github repository
#
# Rendered by Chef - local changes will be replaced

/usr/bin/env ssh -o "StrictHostKeyChecking=no" -i "<%= @deploy_dir %>" $1 $2
